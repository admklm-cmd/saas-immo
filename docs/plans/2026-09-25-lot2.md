# Plan — Lot 2 : finition de l'espace connecté et de l'estimation

Date : 25/09/2026 — Branche locale : feat/ui-finish. Statut : validé par l'utilisateur.
Aucun push, aucune fusion, aucun déploiement. Direction artistique : skill
`.claude/skills/premium-product-motion/SKILL.md` (obligatoire) + `docs/design-system.md`.

## Contraintes
- Aucun changement de base de données, migration, RLS, server action, query, règle métier
  ni garde-fou. Aucune nouvelle dépendance. Aucune donnée, métrique ou texte inventé.
- Aucun badge « Simulation », mention de validation humaine, texte légal ou de consentement
  supprimé ou raccourci.
- Espace connecté = outil de travail : « montrer le fonctionnement » = états réels (en attente,
  validé, bloqué, en retard) rendus par la forme et le mouvement, jamais d'animation décorative.
- Contenu complet sans JavaScript et sous reduced motion (aucune information cachée derrière
  une animation d'apparition). AA, clavier, 1440 / 1024 / 390 px.

## Décisions utilisateur (25/09/2026)
1. Pipeline : les 6 étapes sur une seule ligne, de gauche à droite, défilement horizontal
   natif (tactile, molette, clavier), aucune animation artificielle de défilement.
   « Perdu » reste séparé.
2. Lot 2 en deux temps : 2A → arrêt → validation visuelle (captures avant/après 1440 et
   390 px) → 2B.

## Lot 2A (puis STOP pour validation visuelle)
1. Cadre : menu groupé (Pilotage : Tableau de bord, Contacts, Pipeline, Tâches, Rendez-vous ;
   Agents IA : Vue d'ensemble, Leads entrants, Messages à valider, Relances Emma, Suivi des
   rendez-vous ; Paramètres), icônes de la famille maison, état actif léger avec repère cobalt,
   largeur de page et en-tête éditorial communs, `devIndicators` Next.js désactivé (dev seulement).
2. Tableau de bord (grosse section) : « À faire » compact et aligné ; frise du pipeline comme
   démonstration de l'écran (vrais nombres) ; chiffres existants mis en valeur ; rien de caché
   derrière `Reveal`.
3. Pipeline (grosse section) : une ligne, cartes allégées, « Changer d'étape » discret mais
   accessible, changement d'étape rendu par un mouvement court et motivé ; dialogues de
   confirmation humaine du mandat inchangés.
4. Contacts + fiche contact : tableau équilibré, unités insécables, survol, badges d'étape
   cohérents avec le pipeline ; fiche alignée, historique avec icônes.

## Lot 2B (après validation de 2A)
Messages à valider, Relances, Leads entrants, Suivi des rendez-vous, Rendez-vous, Tâches,
Paramètres (coupe-circuit mis en avant), Estimation (grosse section : scène « comment ça se
passe », consentements mot pour mot). Puis vérifications complètes (tsc, lint, Vitest complet,
Playwright complet, build) et audit cybersecurite de la branche (droit de blocage).
